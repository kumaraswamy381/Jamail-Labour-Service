spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jamail
    username: root
    password: root@123   # change to your DB password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
